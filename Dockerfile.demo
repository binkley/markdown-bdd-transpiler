FROM node:22-alpine

WORKDIR /app

# Cache layer: install only what is needed, skipping the Playwright browser downloads
COPY package*.json ./
RUN npm ci --ignore-scripts

# Copy the demo app
COPY demo-app.js ./

EXPOSE 5173

CMD ["npm", "run", "demo"]

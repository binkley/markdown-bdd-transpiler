{
  "available_steps": [
    {
      "function_name": "navigate_to",
      "description": "Navigates the browser page to a relative URL path.",
      "parameters": ["url_string"]
    },
    {
      "function_name": "enter_into_field",
      "description": "Fills out a form input or text field using its associated label.",
      "parameters": ["text_to_type", "field_label"]
    },
    {
      "function_name": "interact_with_element",
      "description": "Performs an action (e.g., 'click', 'check', 'uncheck') on an element identified by its ARIA role (e.g., 'button', 'link', 'checkbox') and accessible name.",
      "parameters": ["action_type", "aria_role", "accessible_name"]
    },
    {
      "function_name": "verify_element_state",
      "description": "Asserts the state (e.g., 'visible', 'hidden', 'enabled', 'disabled') of an element identified by its ARIA role and accessible name.",
      "parameters": ["expected_state", "aria_role", "accessible_name"]
    }
  ]
}
